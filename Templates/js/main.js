$(document).ready(function() {
	//$.nette.init();
    $('#frm-searchForm').css('width', $('table').css('width'));
	$('.fancybox').fancybox({
		autoSize: true,
		maxHeight: '75%',
		closeClick: false,
		openEffect: 'none',
		closeEffect: 'none'
	});
});